var Rooms = {
  // var messages = parse.readAll();
  // //look thru messages for a list of all unique rooms
  // //default of nothing
  // var roomnames = []
  // $_.map(messages, function (message) {
  //    roomnames.push(message.roomname)
  // })

  // roomnames = $_.uniq(roomnames, false)
  // console.log(roomnames)

  // //pick a room
  //   //and then show only the

};